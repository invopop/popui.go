package examples

import (
	popui "github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ InputWithLabelExample() {
	@popui.Input(props.Input{
		ID:          "email-with-label",
		Label:       "Email",
		Type:        "email",
		Placeholder: "email@example.com",
	})
}
