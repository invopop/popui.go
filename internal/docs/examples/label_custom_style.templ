package examples

import (
	popui "github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ LabelCustomStyleExample() {
	@popui.Label(props.Label{
		ID:    "custom-password",
		Class: "text-foreground-accent text-base font-semibold",
	}) {
		Password
	}
}
