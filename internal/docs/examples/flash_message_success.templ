package examples

import (
	"github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ FlashMessageSuccessExample() {
	@popui.FlashMessage(props.FlashMessage{
		Type:    "success",
		Message: "Saved",
	})
}
