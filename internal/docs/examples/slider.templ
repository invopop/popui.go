package examples

import (
	popui "github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ SliderExample() {
	@popui.Slider(props.Slider{
		Name:  "volume",
		Min:   "0",
		Max:   "100",
		Value: "50",
	})
}
