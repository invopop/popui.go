package examples

import (
	"github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ CodeEditorExample() {
	@popui.CodeEditor(props.CodeEditor{
		Placeholder: "SELECT * FROM users;",
		Rows:        4,
	})
}
