package examples

import (
	popui "github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ FlashMessageCustomMessageExample() {
	@popui.FlashMessage(props.FlashMessage{
		Type:    "success",
		Message: "Profile updated successfully!",
	})
}
