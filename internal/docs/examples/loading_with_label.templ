package examples

import (
	"github.com/invopop/popui.go"
	"github.com/invopop/popui.go/props"
)

templ LoadingWithLabelExample() {
	@popui.Loading(props.Loading{Variant: props.LoadingSpinner, Label: "Loading...", Class: "text-base"})
}
